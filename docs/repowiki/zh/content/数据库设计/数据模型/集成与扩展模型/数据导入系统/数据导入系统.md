# 数据导入系统

<cite>
**本文档引用的文件**  
- [Import.ts](file://server/models/Import.ts)
- [ImportTask.ts](file://server/models/ImportTask.ts)
- [APIImportTask.ts](file://server/queues/tasks/APIImportTask.ts)
- [ImportJSONTask.ts](file://server/queues/tasks/ImportJSONTask.ts)
- [ImportMarkdownZipTask.ts](file://server/queues/tasks/ImportMarkdownZipTask.ts)
- [ImportsProcessor.ts](file://server/queues/processors/ImportsProcessor.ts)
- [types.ts](file://shared/types.ts)
- [schema.ts](file://shared/schema.ts)
</cite>

## 目录
1. [简介](#简介)
2. [核心模型](#核心模型)
3. [导入状态管理](#导入状态管理)
4. [父子任务调度机制](#父子任务调度机制)
5. [API导入任务](#api导入任务)
6. [JSON导入任务](#json导入任务)
7. [Markdown ZIP导入任务](#markdown-zip导入任务)
8. [错误处理与恢复](#错误处理与恢复)
9. [性能优化](#性能优化)
10. [状态图](#状态图)

## 简介
数据导入系统是本平台的核心功能之一，负责从外部服务（如Notion）和本地文件（JSON、Markdown ZIP）导入内容。系统采用分层架构，通过Import和ImportTask模型实现任务的创建、调度和状态管理。导入流程包括分页处理、错误重试、事务管理和进度跟踪，确保大规模数据导入的可靠性和性能。

## 核心模型

### Import模型
Import模型代表一次完整的导入操作，包含导入的元数据、状态和统计信息。每个Import可以包含多个ImportTask，形成父子任务关系。

**Section sources**
- [Import.ts](file://server/models/Import.ts#L23-L87)
- [types.ts](file://shared/types.ts#L64-L71)

### ImportTask模型
ImportTask模型代表导入过程中的单个任务单元，用于实现分页处理和并行执行。每个ImportTask处理一部分数据，并将结果存储在output字段中。

**Section sources**
- [ImportTask.ts](file://server/models/ImportTask.ts#L30-L58)
- [types.ts](file://shared/types.ts#L73-L79)

## 导入状态管理

### Import状态
Import模型使用ImportState枚举管理其生命周期，包含以下状态：
- **Created**: 导入已创建，等待处理
- **InProgress**: 导入正在处理中
- **Processed**: 所有任务已完成，等待持久化
- **Completed**: 导入已完成，数据已持久化
- **Errored**: 导入出错
- **Canceled**: 导入已取消

```mermaid
stateDiagram-v2
[*] --> Created
Created --> InProgress : 开始处理
InProgress --> Processed : 所有任务完成
Processed --> Completed : 持久化数据
InProgress --> Errored : 处理失败
Created --> Canceled : 用户取消
InProgress --> Canceled : 用户取消
```

**Diagram sources**
- [Import.ts](file://server/models/Import.ts#L23-L87)
- [types.ts](file://shared/types.ts#L64-L71)

### ImportTask状态
ImportTask使用ImportTaskState枚举管理其状态，包含：
- **Created**: 任务已创建，等待执行
- **InProgress**: 任务正在执行
- **Completed**: 任务已完成
- **Errored**: 任务执行出错
- **Canceled**: 任务已取消

```mermaid
stateDiagram-v2
[*] --> Created
Created --> InProgress : 开始执行
InProgress --> Completed : 执行成功
InProgress --> Errored : 执行失败
Created --> Canceled : 导入取消
InProgress --> Canceled : 导入取消
```

**Diagram sources**
- [ImportTask.ts](file://server/models/ImportTask.ts#L30-L58)
- [types.ts](file://shared/types.ts#L73-L79)

## 父子任务调度机制

### 任务创建流程
当Import创建时，系统会根据输入数据创建多个ImportTask，实现分页处理：

```mermaid
sequenceDiagram
participant Client
participant API
participant ImportProcessor
participant TaskQueue
Client->>API : 创建Import
API->>ImportProcessor : 触发imports.create事件
ImportProcessor->>ImportProcessor : 构建任务输入
ImportProcessor->>ImportProcessor : 创建多个ImportTask
ImportProcessor->>TaskQueue : 调度第一个ImportTask
TaskQueue-->>ImportProcessor : 任务已调度
ImportProcessor-->>API : 返回响应
```

**Diagram sources**
- [ImportsProcessor.ts](file://server/queues/processors/ImportsProcessor.ts#L162-L217)
- [APIImportTask.ts](file://server/queues/tasks/APIImportTask.ts#L138-L186)

### 任务执行流程
ImportTask按顺序执行，每个任务完成后调度下一个任务：

```mermaid
sequenceDiagram
participant TaskQueue
participant APIImportTask
participant Database
TaskQueue->>APIImportTask : 执行任务
APIImportTask->>Database : 检查导入状态
Database-->>APIImportTask : 返回状态
alt 导入已取消
APIImportTask->>APIImportTask : 设置任务为Canceled
APIImportTask->>Database : 保存状态
else 任务正常执行
APIImportTask->>APIImportTask : 处理数据
APIImportTask->>Database : 创建子任务
APIImportTask->>Database : 保存输出
APIImportTask->>APIImportTask : 调度下一个任务
end
```

**Diagram sources**
- [APIImportTask.ts](file://server/queues/tasks/APIImportTask.ts#L195-L224)
- [ImportsProcessor.ts](file://server/queues/processors/ImportsProcessor.ts#L228-L260)

## API导入任务

### APIImportTask架构
APIImportTask是处理外部API数据导入的基类，采用模板方法模式，定义了标准的处理流程：

```mermaid
classDiagram
class APIImportTask {
+perform(props)
+onFailed(props)
+onProcess(importTask)
+onCompletion(importTask)
+process(importTask)
+scheduleNextTask(importTask)
+uploadAttachments()
+replaceAttachmentUrls()
+options
}
class NotionAPIImportTask {
+process(importTask)
+scheduleNextTask(importTask)
}
APIImportTask <|-- NotionAPIImportTask : 继承
```

**Diagram sources**
- [APIImportTask.ts](file://server/queues/tasks/APIImportTask.ts#L38-L408)
- [NotionAPIImportTask.ts](file://plugins/notion/server/tasks/NotionAPIImportTask.ts#L22-L194)

### 分页处理
系统通过PagePerImportTask常量控制每个任务处理的数据量，避免内存溢出：

```mermaid
flowchart TD
Start([开始]) --> CheckData["检查输入数据"]
CheckData --> HasData{"有数据?"}
HasData --> |是| ChunkData["分块数据 (每块3页)"]
ChunkData --> CreateTasks["创建ImportTask"]
CreateTasks --> ScheduleFirst["调度第一个任务"]
ScheduleFirst --> End([结束])
HasData --> |否| End
```

**Diagram sources**
- [ImportsProcessor.ts](file://server/queues/processors/ImportsProcessor.ts#L162-L217)
- [APIImportTask.ts](file://server/queues/tasks/APIImportTask.ts#L154-L163)

### 错误重试
APIImportTask配置了指数退避重试策略，确保临时性错误能够自动恢复：

```mermaid
flowchart TD
Start([任务执行]) --> Attempt["尝试执行"]
Attempt --> Success{"成功?"}
Success --> |是| Complete["任务完成"]
Success --> |否| CheckAttempts["检查尝试次数"]
CheckAttempts --> HasAttempts{"还有重试机会?"}
HasAttempts --> |是| Wait["等待 (指数退避)"]
Wait --> Attempt
HasAttempts --> |否| HandleFailure["处理失败"]
HandleFailure --> SetErrored["设置为Errored状态"]
Complete --> End([结束])
SetErrored --> End
```

**Diagram sources**
- [APIImportTask.ts](file://server/queues/tasks/APIImportTask.ts#L48-L57)
- [BaseTask.ts](file://server/queues/tasks/BaseTask.ts#L64-L71)

### 事务管理
所有关键操作都在数据库事务中执行，确保数据一致性：

```mermaid
sequenceDiagram
participant Transaction
participant ImportTask
participant Database
Transaction->>Transaction : 开始事务
Transaction->>ImportTask : 处理任务
ImportTask->>Database : 创建子任务
Database-->>ImportTask : 返回结果
ImportTask->>Database : 保存输出
Database-->>ImportTask : 返回结果
ImportTask->>Database : 更新文档计数
Database-->>ImportTask : 返回结果
alt 所有操作成功
Transaction->>Transaction : 提交事务
else 任一操作失败
Transaction->>Transaction : 回滚事务
end
```

**Diagram sources**
- [APIImportTask.ts](file://server/queues/tasks/APIImportTask.ts#L243-L243)
- [ImportsProcessor.ts](file://server/queues/processors/ImportsProcessor.ts#L41-L629)

## JSON导入任务

### JSON导入流程
ImportJSONTask处理JSON格式的导入文件，解析文件结构并转换为系统数据：

```mermaid
flowchart TD
Start([开始]) --> ReadZip["读取ZIP文件"]
ReadZip --> BuildTree["构建文件树"]
BuildTree --> ParseMetadata["解析metadata.json"]
ParseMetadata --> ProcessCollections["处理集合"]
ProcessCollections --> ProcessDocuments["处理文档"]
ProcessDocuments --> ProcessAttachments["处理附件"]
ProcessAttachments --> ReplaceUrls["替换附件URL"]
ReplaceUrls --> End([结束])
```

**Diagram sources**
- [ImportJSONTask.ts](file://server/queues/tasks/ImportJSONTask.ts#L19-L231)

### 文档结构处理
系统维护文档的父子关系，确保导入后的文档结构完整：

```mermaid
classDiagram
class ImportJSONTask {
+parseData(dirPath, fileOperation)
+parseFileTree(tree)
+replaceAttachmentURLs(output)
}
class FileTreeNode {
+path : string
+name : string
+children : FileTreeNode[]
}
class StructuredImportData {
+collections : CollectionData[]
+documents : DocumentData[]
+attachments : AttachmentData[]
}
ImportJSONTask --> FileTreeNode : 使用
ImportJSONTask --> StructuredImportData : 返回
```

**Diagram sources**
- [ImportJSONTask.ts](file://server/queues/tasks/ImportJSONTask.ts#L19-L231)
- [ImportHelper.ts](file://server/utils/ImportHelper.ts#L15-L67)

## Markdown ZIP导入任务

### Markdown导入流程
ImportMarkdownZipTask处理Markdown格式的导入文件，支持复杂的文件结构：

```mermaid
flowchart TD
Start([开始]) --> ReadZip["读取ZIP文件"]
ReadZip --> BuildTree["构建文件树"]
BuildTree --> ProcessRoot["处理根目录"]
ProcessRoot --> IsCollection{"是集合目录?"}
IsCollection --> |是| CreateCollection["创建集合"]
IsCollection --> |否| LogUnhandle["记录未处理文件"]
CreateCollection --> ProcessChildren["处理子节点"]
ProcessChildren --> IsFolder{"是文件夹?"}
IsFolder --> |是| ProcessFolder["处理文件夹"]
IsFolder --> |否| ProcessFile["处理文件"]
ProcessFile --> IsAttachment{"是附件?"}
IsAttachment --> |是| CreateAttachment["创建附件"]
IsAttachment --> |否| CreateDocument["创建文档"]
CreateDocument --> CheckLinks["检查内部链接"]
CheckLinks --> ReplaceLinks["替换链接为占位符"]
ReplaceLinks --> End([结束])
```

**Diagram sources**
- [ImportMarkdownZipTask.ts](file://server/queues/tasks/ImportMarkdownZipTask.ts#L14-L209)

### 附件引用处理
系统自动处理附件引用，将外部URL替换为内部重定向URL：

```mermaid
flowchart TD
Start([开始]) --> FindAttachments["查找所有附件"]
FindAttachments --> CreateModels["创建附件模型"]
CreateModels --> StoreDB["存储到数据库"]
StoreDB --> ScheduleUpload["调度上传任务"]
ScheduleUpload --> WaitUpload["等待上传完成"]
WaitUpload --> ReplaceUrls["替换文档中的URL"]
ReplaceUrls --> End([结束])
```

**Diagram sources**
- [ImportMarkdownZipTask.ts](file://server/queues/tasks/ImportMarkdownZipTask.ts#L14-L209)
- [UploadAttachmentsForImportTask.ts](file://server/queues/tasks/UploadAttachmentsForImportTask.ts#L19-L72)

### 内部链接处理
系统识别并处理文档间的内部链接，确保导入后链接仍然有效：

```mermaid
flowchart TD
Start([开始]) --> FindLinks["查找所有内部链接"]
FindLinks --> ExtractPaths["提取链接路径"]
ExtractPaths --> NormalizePaths["规范化路径"]
NormalizePaths --> FindDocuments["查找目标文档"]
FindDocuments --> HasDocument{"找到文档?"}
HasDocument --> |是| CreatePlaceholder["创建占位符"]
HasDocument --> |否| KeepOriginal["保留原始链接"]
CreatePlaceholder --> ReplaceLinks["替换链接为占位符"]
ReplaceLinks --> End([结束])
```

**Diagram sources**
- [ImportMarkdownZipTask.ts](file://server/queues/tasks/ImportMarkdownZipTask.ts#L14-L209)

## 错误处理与恢复

### 错误处理机制
系统采用分层错误处理策略，确保导入过程的可靠性：

```mermaid
flowchart TD
Start([任务执行]) --> TryProcess["尝试处理"]
TryProcess --> Success{"成功?"}
Success --> |是| Complete["完成任务"]
Success --> |否| CatchError["捕获错误"]
CatchError --> LogError["记录错误信息"]
LogError --> SaveError["保存到ImportTask.error"]
SaveError --> ThrowError["抛出错误"]
ThrowError --> Retry["重试机制"]
Retry --> MaxAttempts{"达到最大尝试次数?"}
MaxAttempts --> |是| HandleFailed["处理失败"]
MaxAttempts --> |否| Wait["等待后重试"]
HandleFailed --> SetErrored["设置Import为Errored"]
SetErrored --> End([结束])
```

**Diagram sources**
- [APIImportTask.ts](file://server/queues/tasks/APIImportTask.ts#L100-L114)
- [ImportsProcessor.ts](file://server/queues/processors/ImportsProcessor.ts#L41-L629)

### 取消机制
用户可以随时取消导入操作，系统会优雅地终止所有相关任务：

```mermaid
sequenceDiagram
participant User
participant API
participant Import
participant ImportTask
User->>API : 取消导入
API->>Import : 设置状态为Canceled
Import->>ImportTask : 查询所有未完成任务
loop 每个未完成任务
ImportTask->>ImportTask : 设置状态为Canceled
ImportTask->>Database : 保存状态
end
ImportTask-->>Import : 所有任务已取消
Import-->>API : 返回响应
API-->>User : 取消成功
```

**Diagram sources**
- [Import.ts](file://server/models/Import.ts#L47-L53)
- [APIImportTask.ts](file://server/queues/tasks/APIImportTask.ts#L196-L199)

## 性能优化

### 批量处理
系统采用批量处理策略，减少数据库交互次数：

```mermaid
flowchart TD
Start([开始]) --> ReadData["读取数据"]
ReadData --> ChunkData["分块数据"]
ChunkData --> ProcessBatch["批量处理"]
ProcessBatch --> SaveBatch["批量保存"]
SaveBatch --> NextBatch{"还有数据?"}
NextBatch --> |是| ProcessBatch
NextBatch --> |否| End([结束])
```

**Diagram sources**
- [APIImportTask.ts](file://server/queues/tasks/APIImportTask.ts#L154-L163)
- [ImportsProcessor.ts](file://server/queues/processors/ImportsProcessor.ts#L162-L217)

### 并行上传
附件上传采用并行处理，提高上传效率：

```mermaid
flowchart TD
Start([开始]) --> FindAttachments["查找所有附件"]
FindAttachments --> CreateModels["创建附件模型"]
CreateModels --> StoreDB["批量存储到数据库"]
StoreDB --> CreateUploadJobs["创建上传任务"]
CreateUploadJobs --> ParallelUpload["并行上传"]
ParallelUpload --> WaitAll["等待所有上传完成"]
WaitAll --> End([结束])
```

**Diagram sources**
- [UploadAttachmentsForImportTask.ts](file://server/queues/tasks/UploadAttachmentsForImportTask.ts#L19-L72)

### 内存优化
系统采用流式处理和分批加载，避免内存溢出：

```mermaid
flowchart TD
Start([开始]) --> OpenStream["打开数据流"]
OpenStream --> ReadChunk["读取数据块"]
ReadChunk --> ProcessChunk["处理数据块"]
ProcessChunk --> SaveChunk["保存处理结果"]
SaveChunk --> HasMore{"还有数据?"}
HasMore --> |是| ReadChunk
HasMore --> |否| CloseStream["关闭数据流"]
CloseStream --> End([结束])
```

**Diagram sources**
- [ImportsProcessor.ts](file://server/queues/processors/ImportsProcessor.ts#L41-L629)
- [ImportTask.ts](file://server/queues/tasks/ImportTask.ts#L105-L555)

## 状态图

### Import状态图
```mermaid
stateDiagram-v2
[*] --> Created
Created --> InProgress : 开始处理
InProgress --> Processed : 所有任务完成
Processed --> Completed : 持久化数据
InProgress --> Errored : 处理失败
Created --> Canceled : 用户取消
InProgress --> Canceled : 用户取消
Errored --> [*]
Completed --> [*]
Canceled --> [*]
```

**Diagram sources**
- [Import.ts](file://server/models/Import.ts#L23-L87)
- [types.ts](file://shared/types.ts#L64-L71)

### ImportTask状态图
```mermaid
stateDiagram-v2
[*] --> Created
Created --> InProgress : 开始执行
InProgress --> Completed : 执行成功
InProgress --> Errored : 执行失败
Created --> Canceled : 导入取消
InProgress --> Canceled : 导入取消
Completed --> [*]
Errored --> [*]
Canceled --> [*]
```

**Diagram sources**
- [ImportTask.ts](file://server/models/ImportTask.ts#L30-L58)
- [types.ts](file://shared/types.ts#L73-L79)

### 完整状态转换图
```mermaid
stateDiagram-v2
[*] --> ImportCreated
state ImportCreated {
[*] --> Created
Created --> InProgress : 开始处理
Created --> Canceled : 用户取消
}
state ImportInProgress {
InProgress --> Processed : 所有任务完成
InProgress --> Errored : 处理失败
InProgress --> Canceled : 用户取消
}
state ImportProcessed {
Processed --> Completed : 持久化数据
Processed --> Errored : 持久化失败
}
state ImportCompleted {
Completed --> [*]
}
state ImportErrored {
Errored --> [*]
}
state ImportCanceled {
Canceled --> [*]
}
state TaskCreated {
Created --> InProgress : 开始执行
Created --> Canceled : 导入取消
}
state TaskInProgress {
InProgress --> Completed : 执行成功
InProgress --> Errored : 执行失败
InProgress --> Canceled : 导入取消
}
state TaskCompleted {
Completed --> [*]
}
state TaskErrored {
Errored --> [*]
}
state TaskCanceled {
Canceled --> [*]
}
ImportCreated --> TaskCreated : 创建任务
ImportInProgress --> TaskInProgress : 执行任务
ImportProcessed --> TaskCompleted : 任务完成
ImportErrored --> TaskErrored : 任务失败
ImportCanceled --> TaskCanceled : 任务取消
```

**Diagram sources**
- [Import.ts](file://server/models/Import.ts#L23-L87)
- [ImportTask.ts](file://server/models/ImportTask.ts#L30-L58)
- [types.ts](file://shared/types.ts#L64-L79)