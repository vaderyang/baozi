# 数据导入机制

<cite>
**本文档引用的文件**   
- [imports.ts](file://server/routes/api/imports/imports.ts)
- [Import.ts](file://app/models/Import.ts)
- [Import.ts](file://server/models/Import.ts)
- [ImportsStore.ts](file://app/stores/ImportsStore.ts)
- [Import.tsx](file://app/scenes/Settings/Import.tsx)
- [ImportsProcessor.ts](file://server/queues/processors/ImportsProcessor.ts)
- [NotionImportsProcessor.ts](file://plugins/notion/server/processors/NotionImportsProcessor.ts)
- [ImportTask.ts](file://server/models/ImportTask.ts)
</cite>

## 目录
1. [简介](#简介)
2. [API端点设计](#api端点设计)
3. [Import模型](#import模型)
4. [异步导入处理](#异步导入处理)
5. [状态管理](#状态管理)
6. [用户界面](#用户界面)
7. [使用示例](#使用示例)

## 简介
数据导入机制允许用户从Markdown和JSON格式导入数据。该系统通过API端点创建导入任务，查询导入列表，获取导入信息和删除导入操作。Import模型存储导入任务的元数据，包括状态、格式、选项和错误信息。ImportsProcessor处理异步导入任务，包括文件解析、文档创建和错误处理。ImportsStore通过ApiClient与后端通信并管理导入状态。Import.tsx组件为用户提供导入界面，支持拖拽上传和文件选择，并展示导入进度和结果。

**Section sources**
- [imports.ts](file://server/routes/api/imports/imports.ts#L1-L185)
- [Import.ts](file://app/models/Import.ts#L1-L57)
- [Import.ts](file://server/models/Import.ts#L1-L90)
- [ImportsStore.ts](file://app/stores/ImportsStore.ts#L1-L25)
- [Import.tsx](file://app/scenes/Settings/Import.tsx#L1-L206)

## API端点设计
导入API的端点设计基于`imports.ts`路由文件，提供了创建导入任务、查询导入列表、获取导入信息和删除导入操作的功能。

```mermaid
flowchart TD
A["创建导入任务\nPOST /imports.create"] --> B["验证用户权限"]
B --> C["检查是否有正在进行的导入"]
C --> D["创建Import模型"]
D --> E["返回导入信息"]
F["查询导入列表\nPOST /imports.list"] --> G["验证用户权限"]
G --> H["查询Import模型列表"]
H --> I["返回导入列表"]
J["获取导入信息\nPOST /imports.info"] --> K["验证用户权限"]
K --> L["查询单个Import模型"]
L --> M["返回导入信息"]
N["删除导入\nPOST /imports.delete"] --> O["验证用户权限"]
O --> P["删除Import模型"]
P --> Q["返回成功信息"]
R["取消导入\nPOST /imports.cancel"] --> S["验证用户权限"]
S --> T["更新Import模型状态为已取消"]
T --> U["返回更新后的导入信息"]
```

**Diagram sources**
- [imports.ts](file://server/routes/api/imports/imports.ts#L1-L185)

**Section sources**
- [imports.ts](file://server/routes/api/imports/imports.ts#L1-L185)

## Import模型
Import模型用于存储导入任务的元数据，包括状态、格式、选项和错误信息。

```mermaid
classDiagram
class Import {
+string name
+string error
+ImportState state
+ImportableIntegrationService service
+number documentCount
+User createdBy
+string createdById
+string integrationId
+string teamId
+cancel() void
}
class User {
+string id
+string email
+string name
}
Import --> User : "createdBy"
```

**Diagram sources**
- [Import.ts](file://app/models/Import.ts#L1-L57)
- [Import.ts](file://server/models/Import.ts#L1-L90)

**Section sources**
- [Import.ts](file://app/models/Import.ts#L1-L57)
- [Import.ts](file://server/models/Import.ts#L1-L90)

## 异步导入处理
ImportsProcessor负责处理异步导入任务，包括文件解析、文档创建和错误处理。

```mermaid
sequenceDiagram
participant Client as "客户端"
participant API as "API服务器"
participant Processor as "ImportsProcessor"
participant DB as "数据库"
Client->>API : 创建导入任务
API->>DB : 创建Import记录
API-->>Client : 返回导入信息
DB->>Processor : 触发imports.create事件
Processor->>Processor : 处理导入任务
Processor->>DB : 更新Import状态
Processor->>Processor : 创建ImportTask
Processor->>Processor : 调度ImportTask
loop 处理每个ImportTask
Processor->>Processor : 执行ImportTask
Processor->>DB : 更新ImportTask状态
Processor->>Processor : 创建文档和集合
end
Processor->>DB : 更新Import状态为已完成
```

**Diagram sources**
- [ImportsProcessor.ts](file://server/queues/processors/ImportsProcessor.ts#L1-L630)
- [NotionImportsProcessor.ts](file://plugins/notion/server/processors/NotionImportsProcessor.ts#L1-L70)

**Section sources**
- [ImportsProcessor.ts](file://server/queues/processors/ImportsProcessor.ts#L1-L630)
- [NotionImportsProcessor.ts](file://plugins/notion/server/processors/NotionImportsProcessor.ts#L1-L70)

## 状态管理
ImportsStore通过ApiClient与后端通信并管理导入状态。

```mermaid
classDiagram
class ImportsStore {
+cancel(importModel : Import) void
}
class Import {
+string name
+string error
+ImportState state
+ImportableIntegrationService service
+number documentCount
+User createdBy
+string createdById
+string integrationId
+string teamId
+cancel() void
}
class ApiClient {
+post(url : string, data : any) Promise<any>
}
ImportsStore --> Import : "管理"
ImportsStore --> ApiClient : "通信"
```

**Diagram sources**
- [ImportsStore.ts](file://app/stores/ImportsStore.ts#L1-L25)

**Section sources**
- [ImportsStore.ts](file://app/stores/ImportsStore.ts#L1-L25)

## 用户界面
Import.tsx组件为用户提供导入界面，支持拖拽上传和文件选择，并展示导入进度和结果。

```mermaid
flowchart TD
A[导入页面] --> B[Markdown导入选项]
A --> C[JSON导入选项]
A --> D[Confluence导入选项]
B --> E[打开Markdown导入对话框]
C --> F[打开JSON导入对话框]
D --> G[显示企业版提示]
A --> H[显示最近导入列表]
H --> I[显示导入项目]
I --> J[显示导入状态]
I --> K[显示导入进度]
```

**Diagram sources**
- [Import.tsx](file://app/scenes/Settings/Import.tsx#L1-L206)

**Section sources**
- [Import.tsx](file://app/scenes/Settings/Import.tsx#L1-L206)

## 使用示例
以下是如何通过API发起Markdown导入任务、查询导入状态和处理错误情况的示例。

### 发起Markdown导入任务
```javascript
// 发起Markdown导入任务
const response = await fetch('/api/imports.create', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    integrationId: 'integration-123',
    service: 'markdown',
    input: [
      {
        externalId: 'page-1',
        permission: 'read_write',
      },
    ],
  }),
});

const result = await response.json();
console.log(result);
```

### 查询导入状态
```javascript
// 查询导入状态
const response = await fetch('/api/imports.info', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    id: 'import-123',
  }),
});

const result = await response.json();
console.log(result);
```

### 处理错误情况
```javascript
// 处理错误情况
try {
  const response = await fetch('/api/imports.create', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({
      integrationId: 'integration-123',
      service: 'markdown',
      input: [
        {
          externalId: 'page-1',
          permission: 'read_write',
        },
      ],
    }),
  });

  if (!response.ok) {
    throw new Error('导入失败');
  }

  const result = await response.json();
  console.log(result);
} catch (error) {
  console.error('导入过程中发生错误:', error.message);
}
```

**Section sources**
- [imports.ts](file://server/routes/api/imports/imports.ts#L1-L185)
- [Import.ts](file://app/models/Import.ts#L1-L57)
- [Import.ts](file://server/models/Import.ts#L1-L90)
- [ImportsStore.ts](file://app/stores/ImportsStore.ts#L1-L25)
- [Import.tsx](file://app/scenes/Settings/Import.tsx#L1-L206)
- [ImportsProcessor.ts](file://server/queues/processors/ImportsProcessor.ts#L1-L630)
- [NotionImportsProcessor.ts](file://plugins/notion/server/processors/NotionImportsProcessor.ts#L1-L70)